# تقرير فحص الكود — مسار

**تاريخ الفحص:** 2026-08-23
**النطاق:** كل الكود الحالي بالمستودع (الواجهة React، قواعد Firestore، Cloudflare Worker، سكربتات الإدارة)
**المنهجية:** فحصين — فحص أول لاكتشاف الأنماط الخطرة عبر كل الملفات (بحث نصي ممنهج + قراءة الملفات الحرجة)، وفحص ثاني للتحقق من كل ملاحظة على الكود الفعلي قبل إدراجها هون (تجنّب البلاغات الكاذبة).

## حالة المتابعة — 2026-08-23

هذا التقرير بدأ كسجل اكتشاف، ثم جرى تنفيذ البنود واحدًا واحدًا على الفرع
`codex/audit-hardening-series` ورفعها إلى GitHub. آخر تحقق آلي: **62/62 اختبار واجهة، 111/111 اختبار قواعد Firestore، lint، build، وWorker typecheck/build**.

المغلق منذ التقرير الأصلي: تجميع علامات الواجبات، فلترة تسليمات المعلم، عزل بنك الأسئلة حسب المادة، حماية المسارات وFeature Flags عند deep-link، سجل التدقيق الموثوق، تحديد معدل Worker، platformStats الموثوق، فحص روابط HTTP(S)، التحقق قبل التصدير، حذف الحسابات عبر Worker، تقسيم الحزمة، رسائل أخطاء الميزات، الاستيراد المتوازي المحدود مع إعادة المحاولة، توحيد مستمع قائمة المستخدمين للمدير، وإشعارات الغياب المتوازية.

المتبقي كقرار تشغيلي أو توسعة لاحقة: حصة Firebase المشتركة ومراقبتها، pagination حقيقية للمدارس الضخمة، وتحويل كل عمليات الكتابة المباشرة إلى Worker. أُغلقت الازدواجية الواضحة في مستمع قائمة المستخدمين، لكن pagination تظل بوابة التوسع التالية. الإنتاج نظيف من الثغرات (`npm audit --omit=dev` = 0)، بينما بقيت 9 ثغرات تطويرية متوسطة لا يصلح علاجها الآمن إلا بتغيير Firebase CLI قسريًا.

الترتيب تحت **حسب الأولوية الفعلية** (تأثير + احتمال حدوث)، مو حسب التصنيف. كل بند معلّم بنوعه: 🔒 أمني / 🐛 تقني (bug) / ⚡ أداء / 📈 استيعاب (capacity) / 🔥 ضغط (load) / 🎨 تصميم.

---

## P0 — حرج، يهدد استقرار المنصة كلها

### 1. 📈🔥 حصة Firestore المجانية مشتركة بين كل المدارس، مو لكل مدرسة لحالها
**أين:** بنية المشروع كلها — `src/firebase.js` فيه مشروع Firebase واحد (`projectId` واحد بمتغيرات البيئة) تستخدمه كل المدارس على المنصة.

**المشكلة:** خطة Firebase Spark المجانية عندها سقف يومي (~50 ألف قراءة، ~20 ألف كتابة، 1GiB تخزين) **على مستوى المشروع كله**، مش لكل مدرسة. يعني لما توصل المنصة لعدد معقول من المدارس النشطة، مدرسة وحدة نشطة كتير (كتير طلاب عم يفتحوا التطبيق بنفس الوقت) ممكن تستهلك الحصة اليومية كاملة، وتوقف الخدمة عن **كل المدارس التانية** بنفس اليوم — مو بس عنها هي.

**ليش هيي أولوية أعلى من أي bug تاني:** أي إصلاح كود ثاني بيصير بلا فايدة إذا المنصة وقفت عن الشغل بسبب استنزاف حصة مشتركة.

**الحل المقترح:**
- مراقبة الاستهلاك اليومي من Firebase Console بشكل دوري (يدويًا لحد ما توفر Backend).
- تقليل الـ listeners الدائمة (`onSnapshot`) لصالح قراءة عند الطلب (`getDocs`) بالأماكن يلي مو محتاجة تحديث لحظي — جزء من هيدا صار فعلاً بمواضع زي `refreshAdminMarks`/`refreshSchoolAbsences`، بس فيه أماكن كتير لسا `onSnapshot` غير ضرورية (شوف البند #2).
- لما يوصل عدد المدارس النشطة لعتبة معينة (حتى لو 3-5 مدارس متوسطة الحجم)، الانتقال لخطة Blaze (pay-as-you-go) صار غير قابل للتأجيل — هاي أكبر قيد بالخطة المجانية الحالية وازم حد أقصى واضح إلها.

---

### 2. ✅ استعلام `submissions` غير المفلتر — مغلق
**أين:** `src/pages/instructor/InstructorGradeHomeworkPage.jsx:33`
```js
const unsub = onSnapshot(collection(db, 'submissions'), (snap) => { ... })
```
**المشكلة:** ما في `where('schoolId', '==', ...)` ولا أي فلتر — الاستعلام هيك بيفتح listener على **كل وثيقة submissions من كل مدرسة على المنصة**. قواعد Firestore بتمنع وصول المعلّم لبيانات مدرسة تانية (كل وثيقة بتتفحص لحالها وبترجع مرفوضة إذا مو من صلاحيته)، بس **كل وثيقة بالمجموعة كلها لسا بتتحسب كقراءة** وقت تقييم القاعدة، حتى لو النتيجة النهائية "مرفوض". يعني:
- كل معلّم فاتح هاي الصفحة عم يكلّف المنصة قراءة كل submission من كل مدرسة، مش بس مدرسته.
- مع نمو عدد المدارس، هاي الصفحة تصير أكبر مستهلك وحيد للحصة اليومية المشتركة (مرتبطة مباشرة بالبند #1).
- نظريًا كمان بتصير أبطأ تدريجيًا (listener على مجموعة متزايدة الحجم).

**ملاحظة:** هذا موجود بالكود من قبل التعديلات الليوم، ما لمسته وقت تطوير دورة الواجبات — بس صار مهم أكتر هلق لأنه المنصة نفسها كبرت.

**الحل:** إضافة `where('schoolId', '==', session.schoolId)` كحد أدنى (تفلترة على مستوى الاستعلام مش بس القاعدة)، والأفضل `where('teacherUid'...)` أو الاعتماد على `homeworkId in [...]` لو ممكن (محدودية Firestore بـ 10 عناصر بـ `in` — ممكن يحتاج تقسيم لعدة استعلامات إذا المعلّم عنده أكتر من 10 واجبات).

---

### 3. ✅ علامة "الواجبات" بكل مادة تُمسح — مغلق
**أين:** `src/context/MarksContext.jsx` (`setMarkValue`) + `src/pages/instructor/InstructorGradeHomeworkPage.jsx`

**المشكلة:** معرّف وثيقة العلامة ثابت: `${studentUid}_${subjectId}_homework` — علامة واحدة **لكل مادة**، مش لكل واجب. لما المعلّم يصحح واجب ثاني بنفس المادة، العلامة القديمة بتنكتب فوقها وتضيع من دفتر الدرجات، رغم إنه التسليم نفسه لسا ظاهر "مصحّح".

**الحالة:** تم رصدها ليوم، وتم فتح مهمة منفصلة إلها (`task_0fa01a40`) — مذكورة هون للتوثيق والأولوية، مو للتنفيذ ضمن هاد الفحص.

**الحل المقترح:** تخزين علامة الواجب لكل `homeworkId` لحاله (معرّف وثيقة مثل `${studentUid}_${subjectId}_homework_${homeworkId}`)، وتجميعها (متوسط أو مجموع) عند حساب فئة "الواجبات" بـ `computeSubjectTotal`.

---

## P1 — عالية، يستاهل معالجة قريبة

### 4. ✅ تحديد معدل Worker، مع حد متبقٍ لكتابات Firestore المباشرة
**أين:** `worker/src/index.ts` (نقطة `create-school-user`)، وبشكل أعم أي كتابة Firestore من المتصفح مباشرة (كل الـ Contexts).

**المشكلة:** حساب مسجّل دخول (حتى لو صلاحياته عادية) يقدر يرسل كمية كبيرة من الكتابات بفترة قصيرة (سكربت بسيط بالمتصفح كافي). ما في أي حد أقصى على معدل الطلبات — لا بالـ Worker ولا بقواعد Firestore. هاد بيفتح باب لاستنزاف الحصة اليومية المشتركة (مرتبط بالبند #1) عن قصد أو غلط (bug بالواجهة يعمل loop لا نهائي مثلًا).

**الحالة الحالية:** أضيفت حدود Cloudflare Worker: 300 طلب/دقيقة لكل IP، و180 عملية/دقيقة لكل فاعل على المسار، وحجم طلب 32KB. ما زالت كتابات Firestore المباشرة من الواجهة بلا عدّاد مركزي؛ الحل الآمن لها مراقبة الاستهلاك أو نقل العمليات الحساسة إلى Worker عند الحاجة.

---

### 5. ✅ بنك الأسئلة خارج مادة المعلم — مغلق
**أين:** `firestore.rules` — قاعدة `questionBank`:
```
allow read: if isSignedIn() && sameSchool(resource.data.schoolId) && (isAdmin() || isInstructor());
```
**المشكلة:** القراءة مقيّدة بالدور (معلّم/إدارة) بس مو بمادة المعلّم نفسها — معلّم رياضيات يقدر يقرا بنك أسئلة معلّم علوم بنفس المدرسة (يشوف الإجابات الصحيحة). خطورته منخفضة (زملاء عمل بنفس المدرسة، مو تسريب لطالب)، بس تقنيًا أوسع من اللازم.

**الحل:** تضييق القراءة لـ `isAdmin() || teachesSubject(resource.data.subjectId)` بدل `isInstructor()` العامة — نفس المنطق المستخدم أصلًا بقاعدة الكتابة لنفس المجموعة.

---

### 6. ✅ حجم ملف JS الرئيسي كبير — مغلق
**أين:** ناتج `npm run build` — تحذير Vite نفسه:
```
dist/assets/index-*.js   ~307 kB │ gzip: ~94 kB
```
**المشكلة:** كل صفحات التطبيق (إدارة، معلّم، طالب، ولي أمر) تنحمّل بملف JS واحد من أول تحميل، بغض النظر شو الدور يلي فتح التطبيق. على إنترنت مدرسي ضعيف (بالضبط السيناريو يلي ميزة PWA/Offline انبنت من أجله)، هاد التحميل الأول بيصير بطيء وبيستهلك بيانات جوال.

**الحل:** `React.lazy()` + `Suspense` على مستوى الصفحات (`src/App.jsx`)، خصوصًا الصفحات الإدارية الكبيرة (`ScheduleOpsPage`, `ExamCenterPage`, `AdminInsightsPage`...) يلي طالب أو ولي أمر عادي ما رح يزورها أبدًا بس لسا منحمّلها إله.

---

### 7. ✅ الاستيراد الجماعي المتسلسل — مغلق جزئيًا
**أين:** `src/context/BulkImportContext.jsx`

**المشكلة:** لكل صف بملف الاستيراد، الكود بيعمل `initializeApp` + `createUserWithEmailAndPassword` + `deleteApp` بشكل متتابع (مش متوازي) — لمدرسة فيها صف استيراد 100-200 طالب، هاي العملية ممكن تاخد وقت طويل (كل حساب بياخد ثانية أو أكتر)، وواجهة التقدم بس "spinner" بدون تفاصيل (كم طالب خلص، كم باقي). كمان Firebase Auth عندها حدود معدل إنشاء حسابات (rate limits) ممكن تنكشف مع أعداد كبيرة.

**الحل:** شريط تقدم فعلي (X من Y)، ومعالجة على دفعات صغيرة متوازية (batch of 5-10) بدل تسلسل واحد-واحد لتسريع العملية مع البقاء تحت حدود Firebase.

---

### 8. 🟡 `npm audit`: ثغرات تطويرية متبقية — غير إنتاجية
**أين:** `package.json` (`react-router-dom`)

**التفاصيل:** `npm audit` بيبلّغ عن ثغرة عالية (CSRF Bypass بـ RSC Mode) بنسخة react-router الحالية. بعد المراجعة: `src/App.jsx` يستخدم `<BrowserRouter>`/`<Routes>`/`<Route>` العادية بس — ما في استخدام لـ React Server Components ولا Data Router (`createBrowserRouter`) ولا `action`/`loader`، يلي هيي بالضبط الأجزاء المتأثرة بالثغرة. يعني **الثغرة موجودة بالحزمة بس مو مستغلّة بالطريقة الحالية لاستخدام المكتبة**.

**الحالة الحالية:** شُغّل الإصلاح الآمن غير القسري، وانخفض العدد إلى 9 ثغرات متوسطة في شجرة التطوير. المتبقي يتطلب `npm audit fix --force` ويستبدل Firebase CLI بشكل breaking، لذلك لم يُنفّذ. `npm audit --omit=dev` نظيف.

---

## P2 — متوسطة، تحسينات مهمة بس مو مستعجلة

### 9. ✅ أخطاء الميزات الجديدة المسكوتة — مغلق للمسارات المدققة
**أين:** `ScheduleOpsContext.jsx`, `ExamCenterContext.jsx`, `InterventionContext.jsx`, `QuestionBankContext.jsx` (الميزات المضافة اليوم)

**المشكلة:** لو صار `permission-denied` أو خطأ شبكة وقت تحميل بيانات (مثلًا قواعد Firestore رفضت الطلب لسبب غير متوقع)، الـ `onSnapshot` بيسجّل الخطأ بـ console بس وبيصفّر الحالة (`setX([])`) بصمت — المستخدم بيشوف "ما في بيانات" بدل رسالة توضح إنه صار خطأ فعلي. `InstructorManualGradesPage.jsx` الموجودة من قبل عندها نمط أفضل (`studentsError` state يظهر للمستخدم) — نفس النمط لازم يتعمم على الميزات الجديدة.

**الحل:** إضافة state خطأ ظاهر بكل context جديد، بدل الاكتفاء بـ console.error.

---

### 10. ✅ فحص بروتوكول الروابط — مغلق
**أين:** `src/utils/parseMaterialUrl.js` + كل مكان بيعرض `materialUrl`/`submission.url` كـ `<a href={...}>`

**المشكلة:** أي رابط يدخله معلّم أو طالب (رابط ملف مرفق، رابط تسليم واجب) بينعرض مباشرة كـ `href` بدون فحص إنه `http`/`https` فقط. المتصفحات الحديثة بتمنع تنفيذ `javascript:` من نقرة مستخدم بمعظم الحالات، بس هاد مو ضمان قاطع بكل الحالات/المتصفحات. خطورة منخفضة (يحتاج المستخدم نفسه يدخل رابط خبيث بحسابه، مش هجوم عن بعد سهل)، بس سهل التصليح.

**الحل:** دالة تحقق بسيطة (`url.startsWith('http://') || url.startsWith('https://')`) قبل قبول أي رابط بأي فورم (مواد الدرس، تسليم الواجب، الخ).

---

### 11. 📈 لا يوجد Pagination على قوائم على مستوى المدرسة كاملة
**أين:** `SchoolStructureContext.jsx` (`allUsers`, `subjects`), `QuestionBankContext.jsx` (`questions`)، وبشكل أعم أغلب الـ Contexts

**المشكلة:** كل قوائم المستخدمين/المواد/الأسئلة تتحمّل كاملة بذاكرة المتصفح بمرة وحدة (`onSnapshot` بدون `limit`). لمدرسة صغيرة-متوسطة (أقل من 500-1000 طالب) هاد مقبول تمامًا. لمدرسة كبيرة جدًا (آلاف الطلاب)، هاد ممكن يبطّئ التحميل الأول وزيادة استهلاك الذاكرة على أجهزة ضعيفة.

**الحالة الحالية:** أزيلت مستمعات قائمة المستخدمين المكررة من صفحات الإدارة وهيكل المدرسة، وصارت تعتمد على المستمع المشترك داخل `SchoolStructureContext`. ما زالت pagination الحقيقية غير مطبقة؛ عند فتح مدارس ضخمة، `limit()` + "تحميل المزيد" هي الخطوة التالية.

---

### 12. ✅ إشعارات الغياب المتسلسلة — مغلق
**أين:** `AttendanceContext.jsx` (إشعارات أولياء الأمور)

**المشكلة:** إرسال إشعار لكل ولي أمر بالتتابع بدل التوازي — تأثير بسيط جدًا (عادة ولي أمر أو اثنين لكل طالب)، بس نمط يستاهل الانتباه إذا تكرر بمكان فيه أعداد أكبر.

**الحل:** `Promise.all(parentUids.map(...))` بدل `for...of` مع `await` بكل تكرار — بس بعد التأكد إنه مو مهم يصير الإرسال بترتيب معيّن (مو الحالة هون).

---

## P3 — منخفضة، ملاحظات جودة كود

### 13. 🎨 استخدام مكثّف لـ inline styles بدل كلاسات CSS
**أين:** كل الصفحات تقريبًا، بما فيها الميزات الجديدة (`ScheduleOpsPage`, `ExamCenterPage`, `StudentProfilePage`...)

**الملاحظة:** نمط موجود بالمشروع من الأساس (مو شي جديد)، بس بيصعّب الصيانة والتناسق البصري مستقبلًا (تغيير لون أو مسافة موحّدة يحتاج تعديل بعشرات الأماكن بدل مكان وحيد بـ `index.css`). مو خطأ، بس ديّن تقني متراكم.

---

### 14. 🎨 تحذيرات `oxlint` حول Fast Refresh بكل ملفات الـ Context (~20 ملف)
**أين:** كل `src/context/*.jsx` تقريبًا

**الملاحظة:** كل ملف بيصدّر الـ Provider (component) + الـ hook (`useX`) من نفس الملف — نمط شائع بالمشاريع الصغيرة/المتوسطة، بس بيكسر Hot Module Reload وقت التطوير بس (0% تأثير بالإنتاج). مو مستعجل.

---

### 15. ✅ `catch` بدون استخدام لمتغيّر الخطأ — مغلق
**أين:** `src/utils/parseStudentsExcel.js:31`, `src/utils/parseTimetableExcel.js:74`

**الملاحظة:** تحذير lint بسيط، الخطأ بيتبلع بصمت بدل ما يتسجّل حتى بـ console — لو صار خطأ فعلي بتحليل ملف Excel، صعب تشخيصه لاحقًا.

---

## أشياء فحصتها وطلعت سليمة (يستاهل توثيقها حتى ما تتراجع لاحقًا)

- **لا يوجد `dangerouslySetInnerHTML`** بكل المشروع — صفر مخاطر XSS من هالجهة.
- **لا يوجد مفاتيح أو أسرار مكتوبة بالكود** (`src/`, `worker/src/`) — كل شي عبر `import.meta.env`/`env.*` أو `wrangler secret`.
- **`serviceAccountKey.json`** محمي صح بـ `.gitignore`، وكل السكربتات الإدارية اللي بتستخدمه شغالة محليًا بس (مو جزء من الواجهة).
- **Cloudflare Worker** (`create-school-user`) بيتحقق من هوية المستخدم (Firebase ID token) وياخد `schoolId` من ملف تعريف المدير نفسه (مش من العميل) — دفاع صحيح ضد انتحال مدرسة تانية.
- **`.env.example`** ما فيه أي قيمة حقيقية، بس أسماء متغيرات فاضية.
- **قواعد Firestore الجديدة** مغطاة باختبارات على محاكي حقيقي (111 اختبارًا ناجحًا) تثبت عزل المدارس عن بعض لكل ميزة جديدة.

---

## الخلاصة التنفيذية

لو عندك وقت لثلاث أشياء بس هالأسبوع: **البند #1** (خطة مراقبة/تصعيد لحصة Firestore المشتركة)، **البند #2** (فلترة استعلام submissions)، و**البند #3** (تصحيح علامة الواجبات المتكررة). هاي الثلاثة مع بعض بتحمي استقرار المنصة كلها وصحة البيانات — الباقي تحسينات مهمة بس مو حرجة بنفس المستوى.
