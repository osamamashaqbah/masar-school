# تقرير تدقيق واجهة مسار

تاريخ التدقيق: 18 أغسطس 2026  
البيئة: بناء إنتاج محلي مطابق للنشر، ثم تحقق مباشر بعد النشر.

## المشاكل المؤكدة والحلول المنفذة

| الأولوية | الملاحظة | السبب الجذري | الحل |
| --- | --- | --- | --- |
| حرجة | الشريط الجانبي يظهر في اليسار على سطح المكتب رغم واجهة RTL | ترتيب مسارات CSS Grid لم يطابق الاتجاه الفعلي للمتصفح | أصبحت شبكة التطبيق تبدأ بشريط عرض ثابت في جهة البداية RTL (اليمين)، والمحتوى في العمود المرن المقابل. |
| حرجة | قائمة أدوات المعلّم/الإدارة تخرج خارج الشاشة وتظهر مقصوصة | قائمة منسدلة مطلقة الموضع كانت تتصادم مع تمرير الشريط الجانبي | استبدلت بالقوائم الثابتة المجمّعة داخل الشريط على سطح المكتب؛ جميع الأدوات مرئية وقابلة للتمرير العمودي. هاتفياً بقيت في لوحة سفلية. |
| عالية | شريط تمرير أفقي داخل التنقل | جمع `overflow-y: auto` مع خروج قائمة منسدلة أفقياً | منع التمرير الأفقي في قائمة الروابط، ووضع اقتطاع نص آمن للعناوين الطويلة. |
| عالية | صفحة الملاحظات الفارغة تحتوي بطاقات فائضة بارتفاع المحادثة الكامل | نمط الرسائل المشترك يفرض ارتفاعاً ثابتاً حتى حين لا توجد حالات | أضفت حالة فارغة مخصصة بارتفاع 240px، مع إبقاء ارتفاع المحادثة الكاملة عند وجود حالة. |

## نطاق الاختبار

### المسارات حسب الدور — سطح المكتب 1080×920

| الدور | المسارات المفحوصة | النتيجة |
| --- | ---: | --- |
| المعلّم | 16 | تحميل صحيح، دون تجاوز أفقي أو نص خطأ |
| الإدارة | 13 | تحميل صحيح، الشريط يميناً، دون تجاوز أفقي أو نص خطأ |
| وليّ الأمر | 7 | تحميل صحيح، الشريط يميناً، دون تجاوز أفقي أو نص خطأ |
| الطالب | 6 | تحميل صحيح، الشريط يميناً، دون تجاوز أفقي أو نص خطأ |
| **الإجمالي** | **42** | **ناجح** |

تمت تغطية الصفحات الرئيسية، الجدول، الاختبارات، الإعلانات، الإعدادات، الرسائل، الملاحظات، أدوات المعلّم، وإدارة المدرسة بما فيها الهيكل والتقارير والتدقيق والتصدير.

### الاستجابة

| القياس | الصفحات المختبرة | النتيجة |
| --- | ---: | --- |
| هاتف 390×844 | 4 | التنقل السفلي ظاهر، لا تجاوز أفقي |
| تابلت 768×1024 | 4 | التنقل السفلي ظاهر، لا تجاوز أفقي |
| سطح عريض 1440×960 | 4 | الشريط الجانبي ظاهر، لا تجاوز أفقي |
| هاتف المعلّم | 1 تفاعل | لوحة «المزيد» فتحت داخل الشاشة وبها 13 رابطاً قابلاً للوصول |

### الإتاحة الأساسية

- لغة الصفحة العربية واتجاه RTL صحيحان.
- رابط تخطي المحتوى موجود ويشير إلى منطقة `main` قابلة للتركيز.
- لا توجد أزرار بلا اسم متاح ولا صور بلا `alt`.
- نمط تركيز مرئي عبر لوحة المفاتيح موجود.

## الاختبارات الآلية

| الفحص | النتيجة |
| --- | --- |
| Lint | ناجح |
| Vitest لتطبيق الواجهة | 9 ملفات / 46 اختباراً ناجحاً |
| قواعد Firestore عبر المحاكي | 105 اختباراً ناجحاً |
| بناء الإنتاج وPWA | ناجح |
| `git diff --check` | ناجح |

## ملاحظة أداء غير مانعة

Vite أبلغ عن حزمة JavaScript مضغوطة أكبر من 500KB. لا يسبب خللاً وظيفياً أو تصميمياً، ولا أضفت تقسيم تحميل كبيراً خارج نطاق إصلاح الواجهة. يمكن متابعة فصل بعض الصفحات لاحقاً إذا ظهر أثر في قياسات الأداء الحقيقية.
