{
  "name": "masar-admin-ops-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit",
    "build": "wrangler deploy --dry-run",
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "jose": "^5"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4"
  }
}
